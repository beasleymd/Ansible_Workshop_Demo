---
# Cisco Facts Role - Default Variables

# Output directory for facts reports
facts_output_dir: "{{ playbook_dir }}/../configs"

# Show facts output during playbook run
show_facts_output: true

# Save facts report to file
save_facts_report: true
