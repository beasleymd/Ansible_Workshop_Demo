<!DOCTYPE html>
<html>
<head>
    <title>{{ inventory_hostname }} - Ansible Workshop Demo</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            background-color: white;
            padding: 30px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        h1 {
            color: #ee0000;
        }
        .info {
            margin-top: 20px;
            padding: 10px;
            background-color: #f9f9f9;
            border-left: 4px solid #ee0000;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to the Ansible Workshop!</h1>
        <p>This web server was configured using Ansible automation.</p>
        
        <div class="info">
            <h3>Server Information:</h3>
            <ul>
                <li><strong>Hostname:</strong> {{ ansible_hostname }}</li>
                <li><strong>IP Address:</strong> {{ ansible_default_ipv4.address | default('N/A') }}</li>
                <li><strong>OS:</strong> {{ ansible_distribution }} {{ ansible_distribution_version }}</li>
                <li><strong>Architecture:</strong> {{ ansible_architecture }}</li>
            </ul>
        </div>
        
        <p style="margin-top: 20px; font-size: 12px; color: #666;">
            Deployed with ❤️ using Red Hat Ansible Automation
        </p>
    </div>
</body>
</html>
